"""Launcher package — split from the monolithic launcher.py.

Public entry points:
- `run.main` — the main() entry point
- `browser._find_browser_for_app_mode` — used by tray.py
- `instance.cleanup_lock_file` — used by tray.py and browser monitor
"""
